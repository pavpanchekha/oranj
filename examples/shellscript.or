
io << "This code is always run\n"

$$main = fn(quiet=false, *args) {
    if not quiet {
        io << "This code only run when the current script is run from the shell\n"
        io << "args contains script arguments: " << args << endl
    }
}