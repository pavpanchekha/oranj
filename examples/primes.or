primes = [2]
curr = 2

#max = input("Maximum? ", int)
max = 250

while curr < max {
    curr += 1
    
    for p in primes {
        if p | curr {
            continue 2
        }
    }

    primes.append(curr)
}

output(join(primes, ", "))

# Uncomment to drop down to console after finishing.
##!drop
