primes = [2]
curr = 2

while curr < 100 {
    curr += 1
    
    for p in primes {
        if p | curr {
            continue 2
        }
    }

    primes.append(curr)
}

output(join(primes, ", "))

#!drop